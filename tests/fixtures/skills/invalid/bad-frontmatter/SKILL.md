---
name: bad-frontmatter
description: [this is broken yaml
  because: the list is never closed
---

This skill has malformed YAML frontmatter. The parser cannot read it.

Expected failure: structure.valid-frontmatter [high]
